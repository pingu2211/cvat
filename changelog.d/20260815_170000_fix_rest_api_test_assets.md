### Fixed

- The REST API test assets now include the `prompt` field of labels and the
  `auto_annotation_function` / `auto_annotation_threshold` fields of projects
  and tasks, which were added without updating the expected responses.
