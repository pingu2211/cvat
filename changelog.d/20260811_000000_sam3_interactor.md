### Added

- SAM3 (Segment Anything 3) as a serverless interactor, supporting text-prompt-based
  segmentation in addition to point/box prompts, on both CPU and GPU deployments.
  Requires a Hugging Face access token with access to `facebook/sam3`, supplied via the
  `HUGGING_FACE_HUB_TOKEN` environment variable at deploy time.
