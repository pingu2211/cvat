### Added

- A "From template" option in the label constructor (available when creating a new project,
  task, or editing labels), letting users pick labels from curated presets (COCO, Vehicles,
  Pedestrians, Animals) instead of typing them manually.
