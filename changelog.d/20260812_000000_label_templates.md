### Added

- A "From template" option in the label constructor (available when creating a new project,
  task, or editing labels), letting users pick labels from a saved template instead of typing
  them manually. Labels can be added one by one or all at once.
