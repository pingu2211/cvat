### Added

- A new optional `prompt` field on labels, editable in the label constructor
  and raw JSON editors. It lets projects and tasks associate a free-text
  prompt with each label, for use by future text-prompt-driven models
  (e.g. SAM3).
