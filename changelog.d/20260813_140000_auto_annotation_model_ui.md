### Added

- The auto annotation model of a project or a task can now be configured from the UI,
  on the project and task detail pages. Previously the `auto_annotation_function` and
  `auto_annotation_threshold` fields could only be set through the REST API.
