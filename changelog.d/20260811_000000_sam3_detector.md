### Added

- SAM3 (Segment Anything 3) as a serverless batch detector, for automatically annotating
  a whole task: for each task label with a configured text prompt, detects every matching
  object instance. Runs on CPU or GPU automatically, and requires the same Hugging Face
  access token as the SAM3 interactor.
